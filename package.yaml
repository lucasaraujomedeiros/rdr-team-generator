name: rdr-team-generator
version: 0.1.0.0

dependencies:
  - base >= 4.7 && < 5

library:
  source-dirs: src
  dependencies:
    - random

executables:
  rdr-team-generator:
    main: Main.hs
    source-dirs: app
    dependencies:
      - random
      - rdr-team-generator


tests:
  project-test:
    main: Specs.hs
    source-dirs: tests
    dependencies:
      - rdr-team-generator
      - hspec
